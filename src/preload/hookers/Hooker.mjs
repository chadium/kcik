/**
 * The purpose of this file is to explain what a Hooker is.
 */
export class Hooker {
  async hook(pimp) {
    // Hooks into the game. This is also the perfect time to expose an API for
    // pimp clients.
  }

  async unhook() {
    // Removes itself from the game.
    // Please clean up after yourself.
  }
}
